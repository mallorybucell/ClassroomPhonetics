<!DOCTYPE html>
<html>
<head>
  <title>ToClassroomPhonetics</title>
  <%= stylesheet_link_tag    'application', media: 'all'%>

  <%= javascript_include_tag 'application' %>
  <%= csrf_meta_tags %>
</head>
<div class="container">
<body>
  <% if current_user %>
    <% if current_user.admin? %>
      <%= render 'admin_nav' %>
    <% elsif current_user.teacher? %>
      <%= render 'teacher_nav' %>
    <% else %>
      <%= render 'student_nav' %>
    <% end %>
  <% else %>
    <% link_to "Log In", new_user_session_path %>
  <% end %>
  <br>
  <% flash.each do |flash_type, message| %>
    <div class="alert alert-<%= translate_to_bootstrap flash_type %>">
      <%= message %>
  <% end %>
  </div>
  <%= yield %>

  <!-- footer -->
  <div class="container">
    <div class="well-foot">
      LingLearn created by Mallory Bucell.  IPA webkeyboard functionality from <a href='http://help.keyman.com/products/web/' class='well-link'>KeymanWeb</a> by Tavultesoft. Base for IPA sound chart from IPA Keyboard by <a class='well-link' href='https://github.com/westonruter/ipa-chart' >Weston Ruter</a>.
    </div>
  </div>
</body>
</div>
</html>

