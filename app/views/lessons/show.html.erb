This is the preview page for lesson <%= @lesson.id %>, <%=@lesson.description %>
Add a back link
Add show course lesson belongs to??
Add show due_date
<table>
  <tbody>
    <% @exercises.each do |ex| %>
    <tr>
      <td><%= ex.stem_text_hash_lookup %></td>
    </tr>
    <tr>
      <td><%= ex.content %></td>
    </tr>
    <tr><td><br><input type="text" /></td></tr>
    <% end %>
  </tbody>
</table>


<div class="container" id="create-assignment">
<h3>Set Lesson as an Individual Assignment</h3>
<%= form_for @assignment, url: assignment_path do |f| %>
  <% #TODO static field showing course? %>
  <%=f.label :due_at, "Due On: " %>
  <%=f.date_field :due_at, :required => true %>
  <br>
  <p>Note: default closing time for assignment is midnight of the due date.</p>
  <br>
  <%=hidden_field_tag :course_id, @lesson.course_id %>
  <%=hidden_field_tag :lesson_id, @lesson.id %>
  <button>Set Assignment</button>
<% end %>

</div>