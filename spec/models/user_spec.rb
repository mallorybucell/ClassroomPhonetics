require 'rails_helper'

it 'can give feedback if teacher'

it 'can view feedback if student'

it 'can create assignments if teacher'

it 'cannot create assignments if student'

it 'can create lesson if teacher'


end
